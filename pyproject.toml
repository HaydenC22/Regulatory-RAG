[tool.ruff]
line-length = 110
target-version = "py312"
exclude = ["corpus/raw", "corpus/processed"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.black]
line-length = 110
target-version = ["py312"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
